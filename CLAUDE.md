# nix-derivations

Read `CONTRIBUTING.md` before changing anything under `pkgs/` or `tests/`.
It carries the conventions this repository enforces and the ones it only
states. Format Nix with `alejandra`; run `bats tests/` before committing.
